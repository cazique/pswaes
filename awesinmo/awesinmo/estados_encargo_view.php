<?php
// This script and data application was generated by AppGini, https://bigprof.com/appgini
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/estados_encargo.php');
	include_once(__DIR__ . '/estados_encargo_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('estados_encargo');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'estados_encargo';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`estados_encargo`.`estado_id`" => "estado_id",
		"`estados_encargo`.`nombre`" => "nombre",
		"`estados_encargo`.`descripcion`" => "descripcion",
		"`estados_encargo`.`orden`" => "orden",
		"`estados_encargo`.`color`" => "color",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`estados_encargo`.`estado_id`',
		2 => 2,
		3 => 3,
		4 => '`estados_encargo`.`orden`',
		5 => 5,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`estados_encargo`.`estado_id`" => "estado_id",
		"`estados_encargo`.`nombre`" => "nombre",
		"`estados_encargo`.`descripcion`" => "descripcion",
		"`estados_encargo`.`orden`" => "orden",
		"`estados_encargo`.`color`" => "color",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`estados_encargo`.`estado_id`" => "Estado id",
		"`estados_encargo`.`nombre`" => "Nombre",
		"`estados_encargo`.`descripcion`" => "Descripcion",
		"`estados_encargo`.`orden`" => "Orden",
		"`estados_encargo`.`color`" => "Color",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`estados_encargo`.`estado_id`" => "estado_id",
		"`estados_encargo`.`nombre`" => "nombre",
		"`estados_encargo`.`descripcion`" => "descripcion",
		"`estados_encargo`.`orden`" => "orden",
		"`estados_encargo`.`color`" => "color",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`estados_encargo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->AllowAdminShowSQL = showSQL();
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'estados_encargo_view.php';
	$x->TableTitle = 'Estados encargo';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`estados_encargo`.`estado_id`';

	$x->ColWidth = [150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Estado id', 'Nombre', 'Descripcion', 'Orden', 'Color', ];
	$x->ColFieldName = ['estado_id', 'nombre', 'descripcion', 'orden', 'color', ];
	$x->ColNumber  = [1, 2, 3, 4, 5, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/estados_encargo_templateTV.html';
	$x->SelectedTemplate = 'templates/estados_encargo_templateTVS.html';
	$x->TemplateDV = 'templates/estados_encargo_templateDV.html';
	$x->TemplateDVP = 'templates/estados_encargo_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: estados_encargo_init
	$render = true;
	if(function_exists('estados_encargo_init')) {
		$args = [];
		$render = estados_encargo_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: estados_encargo_header
	$headerCode = '';
	if(function_exists('estados_encargo_header')) {
		$args = [];
		$headerCode = estados_encargo_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php');
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: estados_encargo_footer
	$footerCode = '';
	if(function_exists('estados_encargo_footer')) {
		$args = [];
		$footerCode = estados_encargo_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php');
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
